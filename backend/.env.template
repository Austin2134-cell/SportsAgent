# Copy to backend/.env and fill in values — DO NOT commit .env
# cp .env.template .env

SUPABASE_URL=https://nlfalrpuspdezfnlakrv.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
# Database URI for migration script (Supabase → Settings → Database → URI)
SUPABASE_DB_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres

ANTHROPIC_API_KEY=
ODDS_API_KEY=          # use NEW rotated key (not the one pasted in chat)
SGO_API_KEY=

ODDS_PRIMARY_SOURCE=sgo
TOA_MONTHLY_QUOTA=20000
TOA_RESERVE=500
TOA_PROPS_MIN=1000
TOA_MAX_CREDITS_PER_SNAPSHOT=150
TOA_MORNING_SNAPSHOT=true
SGO_MONTHLY_OBJECTS=2500
SGO_RESERVE=200
ODDS_POLL_INTERVAL_MINUTES=360
AGENT_SCAN_INTERVAL_MINUTES=180
SNAPSHOT_MAX_AGE_MINUTES=360

TIMEZONE=America/Denver
FRONTEND_URL=https://app.edgebet.com,https://sports-agent-phi.vercel.app

# Google Sheets auto-sync (optional)
# 1. Enable Google Sheets API in Google Cloud
# 2. Create service account → download JSON key
# 3. Share your sheet with the service account email (Editor)
# 4. Sheet ID is the long string in the sheet URL
GOOGLE_SHEET_ID=
GOOGLE_SHEETS_CREDENTIALS_JSON=
GOOGLE_SHEETS_SYNC_EMAIL=austin.noyes21@gmail.com
GOOGLE_SHEETS_TAB_BETS=All Bets
GOOGLE_SHEETS_TAB_RECORD=Overall Record
GOOGLE_SHEETS_TAB_BY_SPORT=Record by Sport
