# Session Detail Files

Optional per-session detail dumps, named `YYYY-MM-DD.md`.

Most sessions only need a one-line entry in the root `CLAUDE.md` Session Log. Create a file
here only when a session is unusually long or complex, and link it from that day's log entry.
